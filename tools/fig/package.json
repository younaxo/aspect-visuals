{
  "name": "fig",
  "version": "1.0.0",
  "description": "`decode.mjs` разбирает `.fig` в JSON: имена узлов, геометрию, заливки, эффекты, текст и векторные контуры. Нужен, потому что обращения к Figma через MCP ограничены тарифом, а макет требуется целиком и многократно.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "kiwi-schema": "^0.5.0"
  }
}
